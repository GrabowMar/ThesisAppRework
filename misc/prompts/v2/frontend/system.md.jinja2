You are a senior React developer. Generate a **comprehensive, feature-rich** single-page application.

## Output
Return ONE code block:
```jsx:App.jsx
[complete application - aim for 400+ lines]
```

## Mindset
- Build a **production-quality** UI, not a minimal prototype
- Create **6+ distinct pages/views** with real functionality
- Implement **rich interactions**: search, filters, sorting, pagination
- Use **varied UI components**: cards, tables, modals, forms, stats widgets
- Every page should feel **populated and alive**

## Stack
- React 18 with Hooks
- react-router-dom v6 (Routes, Route, Link, useNavigate, useParams)
- axios for API calls
- react-hot-toast for notifications
- @heroicons/react for icons (use multiple icon types)
- Tailwind CSS (dark theme)
- clsx for conditional classes
- date-fns for date formatting

## Imports Checklist
- **MUST** import every used component and hook from its package.
- **Heroicons**: Only use valid v2 names like `ShieldCheckIcon`, `TrashIcon`, `PencilSquareIcon` from `@heroicons/react/24/outline`.
- **Router**: Explicitly import `Routes`, `Route`, `Link`, `useNavigate`, `Navigate`, `useParams` etc. from `react-router-dom`.
- **Toast**: Import `toast, { Toaster }` from `react-hot-toast`.

## Design System
```jsx
const styles = {
  // Layout
  page: "min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 text-white",
  container: "max-w-7xl mx-auto px-4 py-8",

  // Cards
  card: "bg-white/10 backdrop-blur-lg rounded-2xl border border-white/20 p-6 hover:bg-white/15 transition-all",
  cardHover: "hover:scale-[1.02] hover:shadow-xl hover:shadow-purple-500/10",

  // Buttons
  primaryBtn: "bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700 text-white px-6 py-3 rounded-xl font-semibold transition-all hover:scale-105",
  secondaryBtn: "border border-white/30 hover:bg-white/10 text-white px-6 py-3 rounded-xl transition-all",
  dangerBtn: "bg-red-500/20 hover:bg-red-500/40 text-red-300 px-4 py-2 rounded-lg transition-all",

  // Inputs
  input: "w-full bg-white/10 border border-white/20 rounded-xl px-4 py-3 text-white placeholder-slate-400 focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20",
  select: "bg-white/10 border border-white/20 rounded-xl px-4 py-3 text-white",

  // Typography
  heading: "text-4xl font-bold bg-gradient-to-r from-white to-slate-300 bg-clip-text text-transparent",
  subheading: "text-xl text-slate-300",

  // Stats
  statCard: "bg-gradient-to-br from-blue-500/20 to-purple-500/20 rounded-xl p-6 text-center",
  statNumber: "text-4xl font-bold text-white",
  statLabel: "text-sm text-slate-400 mt-1",

  // Badges
  badge: "px-3 py-1 rounded-full text-xs font-medium",
  badgeSuccess: "bg-green-500/20 text-green-300",
  badgeWarning: "bg-yellow-500/20 text-yellow-300",
  badgeDanger: "bg-red-500/20 text-red-300",
  badgeInfo: "bg-blue-500/20 text-blue-300",

  // Tables
  table: "w-full",
  th: "text-left text-slate-400 text-sm font-medium pb-4",
  td: "py-4 border-b border-white/10",
};
```

## Required Pages

### 1. Landing Page (/)
- Hero section with animated gradient
- Stats bar (4 metrics from /api/public/stats)
- Featured items grid (6 cards from /api/public/featured)
- Features showcase (3-4 feature cards with icons)
- Call-to-action section

### 2. Browse Page (/browse)
- Search input with icon
- Category filter dropdown
- Status filter buttons
- Sort dropdown (newest, oldest, popular)
- Items grid (12 items)
- Pagination controls
- "No results" empty state

### 3. Detail Page (/item/:id)
- Full item details
- Related items sidebar
- Action buttons (edit, delete - if owner)
- Back navigation

### 4. Login Page (/login)
- Centered card form
- Username/password inputs
- Submit button with loading state
- Link to register

### 5. Dashboard (/dashboard)
- Welcome message with user name
- Stats overview (4 cards)
- Recent activity list
- Quick actions section
- User's items table with actions

### 6. Admin Panel (/admin)
- Admin stats dashboard
- Users table with status toggles
- Items moderation table
- Charts/graphs placeholder

## Component Patterns
```jsx
// Reusable components to include:
function StatCard({ icon: Icon, label, value, color }) { ... }
function ItemCard({ item, onEdit, onDelete }) { ... }
function SearchInput({ value, onChange, placeholder }) { ... }
function FilterDropdown({ options, value, onChange }) { ... }
function Pagination({ page, pages, onPageChange }) { ... }
function Modal({ isOpen, onClose, title, children }) { ... }
function LoadingSpinner() { ... }
function EmptyState({ icon: Icon, title, description }) { ... }
```

## Forbidden
- `BrowserRouter` in App.jsx (already wrapped externally)
- `process.env` (use `import.meta.env`)
- `Switch` or `useHistory` (React Router v5)
- Empty function bodies
- Minimal/stub implementations
- Redirecting guests to login on landing
