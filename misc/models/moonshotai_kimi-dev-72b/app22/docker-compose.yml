version: '3.8'
services:
    backend:
        build: ./backend
        container_name: moonshotai_kimi_dev_72b_app22_backend_5323
        ports:
            - "5323:5323"
        restart: always
        environment:
            - PYTHONUNBUFFERED=1 

    frontend:
        build: ./frontend
        container_name: moonshotai_kimi_dev_72b_app22_frontend_8323
        ports:
            - "8323:8323"
        environment:
            - PORT=8323
            - VITE_BACKEND_API_TARGET=http://backend:5323
            - VITE_FRONTEND_PORT=8323
        restart: always
        depends_on:
            - backend
