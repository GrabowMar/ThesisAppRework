version: '3.8'
services:
    backend:
        build: ./backend
        container_name: thudm_glm_4_32b_app7_backend_5643
        ports:
            - "5643:5643"
        restart: always
        environment:
            - PYTHONUNBUFFERED=1 

    frontend:
        build: ./frontend
        container_name: thudm_glm_4_32b_app7_frontend_8643
        ports:
            - "8643:8643"
        environment:
            - PORT=8643
            - VITE_BACKEND_API_TARGET=http://backend:5643
            - VITE_FRONTEND_PORT=8643
        restart: always
        depends_on:
            - backend
