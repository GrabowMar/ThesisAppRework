# Generate {{ name }} - Backend User Routes

{{ description }}

## Your Task
Implement the **USER-FACING** backend functionality. This includes:
- Database models in `models.py`
- User API routes in `routes/user.py`
- Shared services in `services.py` (if needed)

## User Requirements
{% for req in backend_requirements %}
- {{ req }}
{% endfor %}

{% if api_endpoints %}
## API Endpoints to Implement
{{ api_endpoints }}
{% endif %}

## File Structure (DO NOT CREATE app.py)
The scaffolding already has these files ready for you to fill:
- `models.py` - Define all SQLAlchemy models here
- `routes/user.py` - User API routes (prefix: /api/)
- `services.py` - Shared business logic functions

## IMPORTANT: Routing Prefix Rule
- The `user_bp` blueprint already includes the `/api` prefix.
- Define routes **relative to the blueprint**, e.g. `@user_bp.route('/todos')`.
- Do **NOT** include `/api` inside route decorators (avoid `/api/api/...`).

## Reference
{{ scaffolding_backend_context }}

## Output Format
Generate code in these markdown blocks with EXACT filenames:

**Models (required):**
```python:models.py
# Database models with to_dict() methods
```

**User Routes (required):**
```python:routes/user.py
# User-facing API endpoints
```

**Services (if needed):**
```python:services.py
# Shared business logic
```

**Extra dependencies (if needed):**
```requirements
package-name
```

Generate COMPLETE, WORKING code. Models MUST have `to_dict()` methods.
Routes MUST use `user_bp` blueprint from `routes import user_bp`.

