# {{ name }} - Frontend Application

{{ description }}

## Requirements to Implement

{% for req in frontend_requirements %}
- {{ req }}
{% endfor %}

{% if admin_requirements %}
## Admin Panel Requirements
{% for req in admin_requirements %}
- {{ req }}
{% endfor %}
{% endif %}

{% if api_endpoints %}
## Backend API Available
{{ api_endpoints }}
{% endif %}

## MANDATORY: Minimal File Structure

**You MUST use a flat file structure with minimal files.**

### Required Files (ONLY these):

1. `src/main.jsx` - React entry point (use exact template below)
2. `src/App.jsx` - ALL components and logic in this ONE file
3. `src/App.css` - ALL styles in this ONE file

### src/main.jsx - Use EXACTLY This:

```jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './App.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

### src/App.jsx - Follow This Pattern:

```jsx
import React, { useState, useEffect } from 'react'

const API_BASE = '/api'

// ALL helper components defined in same file
function Loading() {
  return <div className="loading">Loading...</div>
}

function Error({ message }) {
  return <div className="error">{message}</div>
}

// Main App component
function App() {
  const [items, setItems] = useState([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState(null)

  useEffect(() => {
    fetchItems()
  }, [])

  async function fetchItems() {
    try {
      setLoading(true)
      const res = await fetch(`${API_BASE}/items`)
      const data = await res.json()
      setItems(data.items || [])
    } catch (err) {
      setError(err.message)
    } finally {
      setLoading(false)
    }
  }

  if (loading) return <Loading />
  if (error) return <Error message={error} />

  return (
    <div className="app">
      {/* Your UI here */}
    </div>
  )
}

export default App
```

## Output Format

Generate EXACTLY these files:

```jsx:src/main.jsx
// Use the exact template above
```

```jsx:src/App.jsx
// Your COMPLETE application with all components
```

```css:src/App.css
/* All your styles */
```

```json:package.json
{
  "dependencies": {
    "some-lib": "^1.2.3"
  },
  "devDependencies": {
    "some-dev-lib": "^4.5.6"
  }
}
```

## ❌ DO NOT CREATE THESE
- src/components/ directory
- src/hooks/ directory
- src/services/ directory
- src/pages/ directory
- src/context/ directory
- Any subdirectories
- Any additional .jsx files

## Dependencies (Allowed)
- You MAY choose any additional frontend dependencies.
- If you import a package that is not already in the scaffolding, include a `package.json` block in your output.
- The Dependency Healer will reconcile missing packages, but you should still list any new ones you use.

## ✅ REQUIRED
- All components in ONE App.jsx file
- Plain CSS in App.css
- Use fetch() or axios for API calls (if axios, include it in package.json)
- API_BASE = '/api' (Vite proxy handles forwarding)
- useState and useEffect for state

## Implementation Process (DO INTERNALLY)
1. Map requirements → UI sections + state.
2. Map endpoints → fetch calls under `API_BASE`.
3. Implement loading/error/empty states.
4. Keep everything in ONE `src/App.jsx`.

Generate the COMPLETE App.jsx and App.css now with all functionality in minimal files. Do not include explanations.
