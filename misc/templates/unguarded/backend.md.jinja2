# {{ name }} - Backend Application

{{ description }}

## Requirements to Implement

{% for req in backend_requirements %}
- {{ req }}
{% endfor %}

{% if admin_requirements %}
## Admin Features
{% for req in admin_requirements %}
- {{ req }}
{% endfor %}
{% endif %}

{% if api_endpoints %}
## API Endpoints to Create
{{ api_endpoints }}
{% endif %}

## MANDATORY: Single-File Architecture

**You MUST put ALL code in `app.py`** - no other Python files allowed.

### Required Structure in app.py:

```python
from flask import Flask, jsonify, request
from flask_sqlalchemy import SQLAlchemy
from flask_cors import CORS
from datetime import datetime

app = Flask(__name__)
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:////app/data/app.db'
app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False
CORS(app, origins=['*'])
db = SQLAlchemy(app)

# ============== MODELS (define ALL models here) ==============
class YourModel(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    # ... fields ...

# ============== ROUTES (define ALL routes here) ==============
@app.route('/health')
def health():
    return jsonify({'status': 'healthy'})

@app.route('/api/health')
def api_health():
    return jsonify({'status': 'healthy', 'database': 'connected'})

# Add ALL your API routes here...

# ============== APP STARTUP ==============
if __name__ == '__main__':
    with app.app_context():
        db.create_all()
    app.run(host='0.0.0.0', port=5000, debug=True)
```

## Output Format

Generate EXACTLY these files:

```python:app.py
# Your COMPLETE application - models, routes, everything
```

```text:requirements.txt
Flask>=3.0.0
flask-cors>=4.0.0
Flask-SQLAlchemy>=3.1.0
gunicorn>=21.0.0
# Add any other dependencies you use
```

## ❌ DO NOT CREATE THESE FILES
- models.py
- routes.py
- services.py
- config.py
- Any subdirectories
- Any additional .py files

## ✅ REQUIRED
- All code in ONE app.py file
- Both /health AND /api/health endpoints
- SQLite database at /app/data/app.db
- CORS with origins=['*']
- All requirements in requirements.txt

## Dependencies (Allowed)
- You MAY choose any additional Python packages.
- If you import a package that is not already in the scaffolding, include it in `requirements.txt`.
- The Dependency Healer can reconcile missing packages, but you should still list any new ones you use.

## 🔗 API Routing Rules
- Define routes EXACTLY as specified in the API endpoints section above
- Use the full path in @app.route() decorator (e.g., `@app.route('/api/todos')`)
- NO blueprint prefixes needed (this is single-file architecture)
- Include BOTH /health (root) and /api/health (API) endpoints

## Implementation Process (DO INTERNALLY)
1. Map requirements → models + endpoints.
2. Implement models with `to_dict()` and JSON-safe values.
3. Implement routes with validation + correct status codes.
4. Verify `/health` and `/api/health` return 200.
5. Ensure there are no placeholder comments or truncated code.

Generate the COMPLETE app.py now with all functionality in a single file. Do not include explanations.
