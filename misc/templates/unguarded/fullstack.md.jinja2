````jinja
# {{ name }} - Full Stack Application (Unguarded Mode)

{{ description }}

## YOUR MISSION
Implement a **complete, runnable** Flask + React application following the structure below.

## Functional Requirements

### Backend Requirements
{% for req in backend_requirements %}
- {{ req }}
{% endfor %}

### Frontend Requirements
{% for req in frontend_requirements %}
- {{ req }}
{% endfor %}

{% if admin_requirements %}
### Admin Requirements
{% for req in admin_requirements %}
- {{ req }}
{% endfor %}
{% endif %}

{% if api_endpoints %}
## API Endpoints (IMPLEMENT THESE)
{{ api_endpoints }}
{% endif %}

## MANDATORY File Structure

### Backend (EXACTLY these files):
```
backend/
├── app.py           # Flask application with ALL routes
├── models.py        # SQLAlchemy models
├── requirements.txt # Python dependencies
```

### Frontend (EXACTLY these files):
```
frontend/src/
├── main.jsx         # React entry point (use standard template)
├── App.jsx          # Main component with ALL functionality
├── App.css          # All styles
```

## Technical Requirements

### Backend MUST Include:
1. Database: `sqlite:////app/data/app.db` (exactly 4 slashes)
2. CORS: `CORS(app, origins=['*'])`
3. Health endpoints: `/health` and `/api/health`
4. All routes in app.py (no separate route files)
5. Models imported AFTER db is defined

### Frontend MUST Include:
1. API calls using `fetch()` or axios (if axios, include it in frontend/package.json)
2. Error handling with try/catch
3. Loading states
4. Single App.jsx component (no React Router)

## DO NOT USE (Will Cause Build Failures)
- React Router (single page app only)
- Redux/Zustand/Jotai (use useState, useEffect, Context)
- Material UI/Chakra/Ant Design (use plain CSS or Tailwind classes)
- TypeScript (use JavaScript)
- Additional directories (components/, hooks/, services/, routes/)

## Output Format - Generate ALL 6 Files:

```python:backend/app.py
# Complete Flask app with ALL routes
```

```python:backend/models.py
# All SQLAlchemy models
```

```text:backend/requirements.txt
# Dependencies (Flask, flask-cors, Flask-SQLAlchemy, gunicorn, plus any extras you use)
```

```jsx:frontend/src/main.jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './App.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

```jsx:frontend/src/App.jsx
// Complete React application
```

```css:frontend/src/App.css
/* All styles */
```

```json:frontend/package.json
{
  "dependencies": {
    "some-lib": "^1.2.3"
  },
  "devDependencies": {
    "some-dev-lib": "^4.5.6"
  }
}
```

## Quality Requirements
- **Complete code** - No placeholders, no TODOs, no "// add more here"
- **Working code** - Must compile and run without errors
- **Error handling** - All API calls wrapped in try/catch
- **Loading states** - Show loading during API calls
- **Professional UI** - Clean, functional appearance

## Dependencies (Allowed)
- You MAY choose any additional Python or frontend packages.
- If you import a package that is not already in the scaffolding, include it in `backend/requirements.txt` or `frontend/package.json`.
- The Dependency Healer will reconcile missing packages, but you should still list any new ones you use.

Generate ALL 6 files now with COMPLETE, RUNNABLE code.
````