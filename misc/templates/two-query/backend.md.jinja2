# Generate {{ name }} - Backend

{{ description }}

## Requirements
{% for req in backend_requirements %}
- {{ req }}
{% endfor %}

## API Endpoints (MUST IMPLEMENT EXACTLY)
{% if api_endpoints %}
{{ api_endpoints }}

**CRITICAL**: Frontend will call these EXACT paths. Do not change endpoint names.
{% endif %}

## Technical Reference
{{ scaffolding_backend_context }}

## MUST FOLLOW
1. **Database**: `sqlite:////app/data/app.db` (exactly 4 slashes)
2. **Code order**: imports → app setup → decorators → models → db.create_all() → routes → main
3. **Only implement requirements**: Do NOT add authentication unless requirements specify it
4. **List response format**: `{"items": [...], "total": N}`
5. **IMPORTANT**: Replace example placeholders like `/api/YOUR_RESOURCE` with ACTUAL paths from requirements above (e.g., `/api/todos`)

## Output

Output the **complete Python code** for `app.py` in a single code block.


