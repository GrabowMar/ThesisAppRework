# Generate {{ name }} - Backend

{{ description }}

## Requirements
{% for req in backend_requirements %}
- {{ req }}
{% endfor %}

## API Endpoints (MUST IMPLEMENT EXACTLY)
{% if api_endpoints %}
{{ api_endpoints }}

**CRITICAL**: Frontend will call these EXACT paths. Do not use different endpoint names.
{% endif %}

## Scaffolding & Technical Constraints
{{ scaffolding_backend_context }}

## Quality Standards

- **Validation**: Validate all input data. Return 400 for invalid inputs.
- **Error Handling**: Wrap database operations in try/except blocks. Return 500 for server errors.
- **Pagination**: Implement pagination for list endpoints (default 20 items).
- **Search/Filtering**: Add query parameter filtering for list endpoints where appropriate.
- **Documentation**: Add docstrings to all functions and classes.
- **Comments**: Explain complex logic.

## Output

Output the **complete Python code** for `app.py` in a single code block. Do not include markdown outside the code block.


