# Generate {{ name }} - Backend

{{ description }}

## Requirements
{% for req in backend_requirements %}
- {{ req }}
{% endfor %}

## API Endpoints
{% if api_endpoints %}
{{ api_endpoints }}

**Note**: Frontend will call these paths. Use them as specified.
{% endif %}

## Technical Reference
{{ scaffolding_backend_context }}

## Guidelines
1. **Database**: `sqlite:////app/data/app.db` (4 slashes for absolute path)
2. **List response format**: `{"items": [...], "total": N}`
3. **File uploads**: Save to `/app/data/uploads/` directory
4. **No authentication** unless requirements explicitly specify it

## What You Can Generate
- **app.py** (required): Main application with routes and models
- **Additional Python files**: models.py, routes.py, utils.py, services/*.py as needed
- **requirements.txt additions**: Mention any extra packages needed

## Output Format
- Main code: ```python block
- Additional files: ```python:filename.py blocks
- Extra requirements: ```requirements block

Generate complete, working code that implements ALL requirements.


