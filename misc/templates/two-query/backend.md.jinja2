# Generate {{ name }} - Backend

{{ description }}

## Requirements
{% for req in backend_requirements %}
- {{ req }}
{% endfor %}

## API Endpoints (MUST IMPLEMENT EXACTLY)
{% if api_endpoints %}
{{ api_endpoints }}

**CRITICAL**: Frontend will call these EXACT paths. Do not use different endpoint names.
{% endif %}

## Scaffolding & Technical Constraints
{{ scaffolding_backend_context }}

- **Persistence**: Use absolute path `sqlite:////app/data/app.db` for SQLite database.

## Quality Standards

- **Validation**: Validate all input data. Return 400 with `{"error": "message"}` for invalid inputs.
- **Error Handling**: Use `@handle_errors` decorator pattern. Return 500 for server errors.
- **Pagination**: Implement pagination for list endpoints (default 20, max 100 items).
- **Filtering**: Add query parameter filtering for list endpoints where appropriate.
- **Logging**: Use Python's logging module for important operations (create, update, delete).
- **Models**: All models must have `to_dict()` method for JSON serialization.
- **Health Check**: Always include `/api/health` endpoint returning `{"status": "healthy"}`.

## Output

Output the **complete Python code** for `app.py` in a single code block. Do not include markdown outside the code block.


