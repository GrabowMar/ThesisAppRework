# Generate {{ name }} - Frontend

{{ description }}

## Requirements
{% for req in frontend_requirements %}
- {{ req }}
{% endfor %}

## What Scaffold Provides
React 18.2, Axios 1.6, Bootstrap 5.3.2, Vite 5.0, `index.html`, `main.jsx`, `App.css`

**Your code replaces `src/App.jsx` entirely.**

## Backend API
Backend runs at `http://backend:5000` (Docker container, don't use localhost)
- `GET /health` - Health check
- Additional endpoints per requirements

## Critical Constraints

**API URL:** Must use `const API_URL = 'http://backend:5000';` (Docker networking)

**No mock data:** Fetch all dynamic content from backend API

**UI states:** Show loading, error, and success states visually

**Use Axios:** For all HTTP requests (not fetch)

**Modern React:** Functional components, hooks (`useState`, `useEffect`, `useCallback`)

**Must implement:**
- Error handling (try/catch, user-friendly messages)
- Form validation (clear errors, disable during submit)
- Loading indicators, success/error feedback
- Responsive design (Bootstrap classes)
- Complete functionality (no TODOs/placeholders)

**Output only JSX code for App.jsx**

## Pattern

```jsx
import React, { useState, useEffect } from 'react';
import axios from 'axios';
import 'bootstrap/dist/css/bootstrap.min.css';

const API_URL = 'http://backend:5000';

function App() {
  const [items, setItems] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchData = async () => {
      try {
        const response = await axios.get(`${API_URL}/api/items`);
        setItems(response.data.items || []);
      } catch (err) {
        setError(err.response?.data?.error || 'Failed to load');
      } finally {
        setLoading(false);
      }
    };
    fetchData();
  }, []);

  if (loading) return <div className="container mt-5">Loading...</div>;
  if (error) return <div className="container mt-5 alert alert-danger">{error}</div>;

  return (
    <div className="container mt-4">
      <h1>Items</h1>
      {items.map(item => <div key={item.id}>{item.name}</div>)}
    </div>
  );
}

export default App;
```
