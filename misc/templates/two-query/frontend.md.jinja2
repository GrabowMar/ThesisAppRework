# Generate {{ name }} - Frontend

{{ description }}

## Requirements
{% for req in frontend_requirements %}
- {{ req }}
{% endfor %}

## Backend API
Backend API is available at `/api/*` paths (proxied by Nginx/Vite to backend container)

**CRITICAL: Use EXACT endpoint paths below. Frontend and backend MUST match.**

{% if api_endpoints %}
{{ api_endpoints }}

**Do not use different endpoint paths (like /api/items instead of /api/todos).**
{% else %}
- Additional endpoints per requirements
{% endif %}

## Scaffolding & Technical Constraints
{{ scaffolding_frontend_context }}

## Quality Standards

- **UI/UX**:
   - Use Bootstrap 5 components (Cards, Modals, Alerts, Badges).
   - Implement **Loading States** (spinners) for all async operations.
   - Implement **Error Handling** (dismissible alerts) for failed requests.
   - Ensure **Responsive Design** (mobile-friendly).
- **Functionality**:
   - Implement full CRUD if the API supports it.
   - Add form validation (client-side) before submitting.
   - Auto-refresh lists after updates.
- **Code Quality**:
   - Clean, readable code.
   - meaningful variable names.
   - Handle edge cases (empty lists, 404s).

## Output

Output the **complete JSX code** for `App.jsx` in a single code block. Do not include markdown outside the code block.

