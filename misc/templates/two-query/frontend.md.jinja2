# Generate {{ name }} - Frontend

{{ description }}

## Requirements
{% for req in frontend_requirements %}
- {{ req }}
{% endfor %}

## Backend API (USE EXACT PATHS)
{% if api_endpoints %}
{{ api_endpoints }}

**CRITICAL**: Use these EXACT endpoint paths. Backend implements them exactly as shown.
{% endif %}

## Technical Reference
{{ scaffolding_frontend_context }}

## MUST FOLLOW
1. **API paths**: Replace `/api/YOUR_RESOURCE` with EXACT paths from API section (e.g., `/api/todos`)
2. **Available components**: ONLY `Spinner` and `ErrorBoundary` exist in `./components`
3. **List response**: Backend returns `{items: [...], total: N}` - use `response.data.items`
4. **Delete confirmation**: Use `window.confirm()` before delete operations
5. **No routing/auth** unless requirements specify it

## QUALITY EXPECTATIONS
Write production-ready React code that includes:
- **Loading states**: Show spinner during data fetching and form submission
- **Error handling**: Display error messages, include retry button for failed loads
- **Form validation**: Validate inputs before submit, show field-specific errors
- **Empty states**: Helpful message when no items exist
- **Toast notifications**: Success/error feedback for user actions
- **Search/filter**: Allow users to filter the list (if applicable)
- **Inline editing**: Edit items without navigating away (if update is required)
- Keep it pragmatic - good UX without over-engineering

## Output

Output the **complete JSX code** for `App.jsx` in a single code block.


