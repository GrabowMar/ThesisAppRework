# Generate Frontend: {{ name }}

{{ description }}

## Requirements

### User Features
{% for req in frontend_requirements %}
- {{ req }}
{% endfor %}

### Admin Features
{% for req in admin_requirements %}
- {{ req }}
{% endfor %}

## Backend API (Generated)

{{ backend_api_context }}

## Stack
- React 18 + Vite
- Tailwind CSS for styling
- axios for API calls
- react-hot-toast for notifications
- react-router-dom for routing

## Available Packages (DO NOT ADD OTHERS)
react, react-dom, react-router-dom, axios, react-hot-toast, @heroicons/react, date-fns, clsx, uuid

## SINGLE FILE OUTPUT

Generate ONE complete App.jsx file with ALL code:
- API client (axios instance + API functions)
- Auth context (AuthProvider + useAuth hook)
- Public HomePage component (guest content + logged-in summary)
- LoginPage component
- UserPage component
- AdminPage component
- Navigation component
- ProtectedRoute component
- Main App component with Routes (NO BrowserRouter - main.jsx handles that)

**CRITICAL: Do NOT wrap App in BrowserRouter. main.jsx already provides it.**

Auth behavior requirements:
- Store JWT in localStorage under key "token"
- After register/login, set user state from response (token + user)
- On app load, call /api/auth/me if token exists

User content requirements:
- Show a public/guest message on the Home page when not authenticated
- Show a "Logged in as <username>" panel on the Home page when authenticated
- / route MUST be public (HomePage)
- /user route MUST be protected

## Code Patterns

### API Client
```jsx
const baseURL = import.meta.env.VITE_BACKEND_URL 
  ? `${import.meta.env.VITE_BACKEND_URL.replace(/\/$/, '')}/api` 
  : '/api';
const api = axios.create({ baseURL });
api.interceptors.request.use(c => {
  const t = localStorage.getItem('token');
  if (t) c.headers.Authorization = `Bearer ${t}`;
  return c;
});
```

### Auth Functions
```jsx
const login = async (username, password) => {
  const res = await api.post('/auth/login', { username, password });
  if (res.data.token) localStorage.setItem('token', res.data.token);
  return res.data;
};
```

### Component Pattern
```jsx
function UserPage() {
  const [items, setItems] = useState([]);
  const [loading, setLoading] = useState(true);
  
  useEffect(() => {
    fetchItems();
  }, []);
  
  if (loading) return <div>Loading...</div>;
  return (/* JSX */);
}
```

### Toast Usage
```jsx
toast.success('Saved!');
toast.error(err.response?.data?.error || 'Error');
```

## Output Format

Generate ONE file only:

```jsx:App.jsx
// Complete React application with all components
```

If you need additional dependencies, mention them but they must be from the available packages list.

Generate complete, working React code. No placeholders. No explanations.


