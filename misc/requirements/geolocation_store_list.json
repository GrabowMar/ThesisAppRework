{
  "slug": "geolocation_store_list",
  "name": "Store Finder",
  "category": "Geolocation",
  "description": "Find stores by location",
  "backend_requirements": [
    "1. Store model: id, name, address, latitude, longitude, phone",
    "2. GET /api/stores - return all stores",
    "3. GET /api/stores/nearby?lat=X&lng=Y&radius=5 - filter by distance",
    "4. Seed with 5-10 sample store locations"
  ],
  "frontend_requirements": [
    "1. Store list showing name, address, phone",
    "2. Display distance from user location (if permission granted)",
    "3. Optional: simple map using Leaflet.js showing store markers",
    "4. Click store to view details",
    "5. 'Get Directions' button with link to Google Maps"
  ],
  "api_endpoints": [
    {
      "method": "GET",
      "path": "/api/stores",
      "description": "List all stores"
    },
    {
      "method": "POST",
      "path": "/api/stores",
      "description": "Create new store"
    },
    {
      "method": "PUT",
      "path": "/api/stores/:id",
      "description": "Update store"
    },
    {
      "method": "DELETE",
      "path": "/api/stores/:id",
      "description": "Delete store"
    }
  ],
  "control_endpoints": [
    {
      "method": "GET",
      "path": "/api/health",
      "description": "Health check endpoint"
    }
  ]
}