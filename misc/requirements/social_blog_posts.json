{
  "slug": "social_blog_posts",
  "name": "Blog",
  "category": "Social",
  "description": "Write and read blog posts",
  "backend_requirements": [
    "1. Post model: id, title (required), content (text), author, created_at, updated_at",
    "2. CRUD endpoints: GET /api/posts (list), GET /api/posts/:id (single), POST, PUT, DELETE",
    "3. Support pagination: ?page=1&limit=10",
    "4. Search posts: ?search=query"
  ],
  "frontend_requirements": [
    "1. Post list showing title, excerpt (first 200 chars), date, author",
    "2. Click post title to view full content",
    "3. Create/edit post form with title and content textarea",
    "4. Delete button with confirmation",
    "5. Pagination controls at bottom"
  ],
  "api_endpoints": [
    {
      "method": "GET",
      "path": "/api/posts",
      "description": "List all posts (supports ?page=, ?limit=, ?search= parameters)"
    },
    {
      "method": "GET",
      "path": "/api/posts/:id",
      "description": "Get single post by ID"
    },
    {
      "method": "POST",
      "path": "/api/posts",
      "description": "Create new post"
    },
    {
      "method": "PUT",
      "path": "/api/posts/:id",
      "description": "Update post"
    },
    {
      "method": "DELETE",
      "path": "/api/posts/:id",
      "description": "Delete post"
    },
    {
      "method": "GET",
      "path": "/api/health",
      "description": "Health check endpoint"
    }
  ]
}