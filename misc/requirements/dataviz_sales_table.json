{
  "slug": "dataviz_sales_table",
  "name": "Sales Dashboard",
  "category": "Data Visualization",
  "description": "View and visualize sales data",
  "backend_requirements": [
    "1. Sale model: id, date, amount (decimal), category, description",
    "2. GET /api/sales - return all sales, support date range filter",
    "3. GET /api/sales/summary - return totals by category and month",
    "4. Seed with sample data if database is empty"
  ],
  "frontend_requirements": [
    "1. Sales table with date, amount, category, description columns",
    "2. Total sales displayed prominently at top",
    "3. Simple bar chart showing sales by category (Chart.js or similar)",
    "4. Date range filter (this month, last month, custom)",
    "5. Export to CSV button"
  ],
  "api_endpoints": [
    {
      "method": "GET",
      "path": "/api/saless",
      "description": "List all saless"
    },
    {
      "method": "POST",
      "path": "/api/saless",
      "description": "Create new sales"
    },
    {
      "method": "PUT",
      "path": "/api/saless/:id",
      "description": "Update sales"
    },
    {
      "method": "DELETE",
      "path": "/api/saless/:id",
      "description": "Delete sales"
    }
  ],
  "control_endpoints": [
    {
      "method": "GET",
      "path": "/api/health",
      "description": "Health check endpoint"
    }
  ]
}