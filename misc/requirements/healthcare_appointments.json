{
  "slug": "healthcare_appointments",
  "name": "Appointment Scheduler",
  "category": "Healthcare",
  "description": "Book and manage appointments",
  "backend_requirements": [
    "Appointment model: id, patient_name (required), date (required), time (required), doctor, notes",
    "CRUD endpoints: GET /api/appointments, POST, PUT, DELETE",
    "Filter by date: ?date=YYYY-MM-DD",
    "Validate: no double-booking same time slot"
  ],
  "frontend_requirements": [
    "Appointment list grouped by date, sorted by time",
    "Book form with patient name, date picker, time picker, doctor dropdown, notes",
    "Click appointment to view/edit details",
    "Cancel button with confirmation",
    "Today's appointments highlighted"
  ],
  "api_endpoints": [
    {
      "method": "GET",
      "path": "/api/appointmentss",
      "description": "List all appointmentss"
    },
    {
      "method": "POST",
      "path": "/api/appointmentss",
      "description": "Create new appointments"
    },
    {
      "method": "PUT",
      "path": "/api/appointmentss/:id",
      "description": "Update appointments"
    },
    {
      "method": "DELETE",
      "path": "/api/appointmentss/:id",
      "description": "Delete appointments"
    }
  ],
  "control_endpoints": [
    {
      "method": "GET",
      "path": "/api/health",
      "description": "Health check endpoint"
    }
  ]
}