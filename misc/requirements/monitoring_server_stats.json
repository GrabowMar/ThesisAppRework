{
  "slug": "monitoring_server_stats",
  "name": "Server Monitor",
  "category": "Monitoring",
  "description": "Display server metrics",
  "backend_requirements": [
    "GET /api/stats - return {cpu_percent, memory_percent, disk_percent, uptime_seconds}",
    "Use psutil library or mock data if not available",
    "GET /api/stats/history - return last 10 readings",
    "Update readings every 5 seconds"
  ],
  "frontend_requirements": [
    "Large cards showing CPU%, Memory%, Disk%, Uptime",
    "Color-coded indicators: green (<70%), yellow (70-90%), red (>90%)",
    "Simple line chart showing history",
    "'Refresh' button to update immediately",
    "Auto-refresh every 10 seconds"
  ],
  "api_endpoints": [
    {
      "method": "GET",
      "path": "/api/servers",
      "description": "List all servers"
    },
    {
      "method": "POST",
      "path": "/api/servers",
      "description": "Create new server"
    },
    {
      "method": "PUT",
      "path": "/api/servers/:id",
      "description": "Update server"
    },
    {
      "method": "DELETE",
      "path": "/api/servers/:id",
      "description": "Delete server"
    }
  ],
  "control_endpoints": [
    {
      "method": "GET",
      "path": "/api/health",
      "description": "Health check endpoint"
    }
  ]
}