{
  "slug": "booking_reservations",
  "name": "Reservation System",
  "category": "Booking",
  "description": "Book time slots or rooms",
  "backend_requirements": [
    "1. Reservation model: id, name (required), resource (room/table/etc), date (required), time_slot, guests",
    "2. CRUD endpoints: GET /api/reservations, POST, PUT, DELETE",
    "3. GET /api/reservations/available?date=X&resource=Y - show available time slots",
    "4. Validate: prevent double-booking same resource/time"
  ],
  "frontend_requirements": [
    "1. Reservation list grouped by date, showing name, resource, time",
    "2. Book form: name, resource dropdown, date picker, time slot picker, guests",
    "3. Show available slots when date/resource selected",
    "4. Cancel reservation button",
    "5. Today's reservations highlighted"
  ],
  "api_endpoints": [
    {
      "method": "GET",
      "path": "/api/reservationss",
      "description": "List all reservationss"
    },
    {
      "method": "POST",
      "path": "/api/reservationss",
      "description": "Create new reservations"
    },
    {
      "method": "PUT",
      "path": "/api/reservationss/:id",
      "description": "Update reservations"
    },
    {
      "method": "DELETE",
      "path": "/api/reservationss/:id",
      "description": "Delete reservations"
    }
  ],
  "control_endpoints": [
    {
      "method": "GET",
      "path": "/api/health",
      "description": "Health check endpoint"
    }
  ]
}