{
  "slug": "crm_customer_list",
  "name": "Customer Directory",
  "category": "CRM",
  "description": "Manage customer contacts",
  "backend_requirements": [
    "Customer model: id, name (required), email (required, unique), phone, company, notes",
    "CRUD endpoints: GET /api/customers, POST, PUT, DELETE",
    "Search customers: ?query=text searches name, email, company",
    "Email validation on create/update"
  ],
  "frontend_requirements": [
    "Customer list table: name, email, phone, company",
    "Add customer form with validation",
    "Click customer to view/edit details",
    "Delete button with confirmation",
    "Search box to filter customers"
  ],
  "api_endpoints": [
    {
      "method": "GET",
      "path": "/api/customers",
      "description": "List all customers"
    },
    {
      "method": "POST",
      "path": "/api/customers",
      "description": "Create new customer"
    },
    {
      "method": "PUT",
      "path": "/api/customers/:id",
      "description": "Update customer"
    },
    {
      "method": "DELETE",
      "path": "/api/customers/:id",
      "description": "Delete customer"
    }
  ],
  "control_endpoints": [
    {
      "method": "GET",
      "path": "/api/health",
      "description": "Health check endpoint"
    }
  ]
}