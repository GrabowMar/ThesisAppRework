# Test requirements for Thesis Research App
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest>=7.0.0
pytest-flask>=1.2.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-xdist>=3.0.0  # For parallel test execution
pytest-html>=3.1.0   # For HTML test reports
pytest-json-report>=1.5.0  # For JSON test reports

# Test utilities
factory-boy>=3.2.0   # For creating test fixtures
faker>=18.0.0        # For generating fake data
responses>=0.23.0    # For mocking HTTP requests
freezegun>=1.2.0     # For time-based testing
mock>=4.0.0          # Additional mocking utilities

# Coverage and quality
coverage>=7.0.0
pytest-benchmark>=4.0.0  # For performance testing
pytest-timeout>=2.1.0    # For test timeouts
pytest-random-order>=1.1.0  # For randomized test order

# Debugging and analysis
pytest-pdb>=0.2.0    # For debugging tests
pytest-profiling>=1.7.0  # For profiling tests
pytest-clarity>=1.0.0    # For better test output

# Documentation testing
pytest-doctest>=0.2.0

# Database testing
pytest-postgresql>=4.1.0  # For PostgreSQL testing (optional)
pytest-redis>=2.4.0       # For Redis testing (optional)

# Async testing (if needed)
pytest-asyncio>=0.21.0

# Web testing
selenium>=4.0.0      # For browser automation
playwright>=1.30.0   # Alternative to Selenium
requests-mock>=1.10.0  # For mocking requests

# Performance and load testing
locust>=2.0.0        # Already used in main app
memory-profiler>=0.60.0  # For memory profiling

# Security testing
bandit>=1.7.0        # Security linting (already used)
safety>=2.3.0        # Vulnerability checking (already used)

# Code quality (for testing code quality in tests)
flake8>=6.0.0
black>=23.0.0
isort>=5.12.0

# Type checking
mypy>=1.0.0
types-requests>=2.31.0
types-flask>=1.1.0
